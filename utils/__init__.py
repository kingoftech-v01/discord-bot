"""
Utility modules shared across the bot.

Currently contains:
    - ``database.py`` -- Async SQLite database wrapper (singleton ``db`` instance)
      used by all cogs and the Django dashboard for data persistence.
"""
